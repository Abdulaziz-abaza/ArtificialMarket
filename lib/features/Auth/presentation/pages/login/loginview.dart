import 'package:artificial_app/features/Auth/presentation/pages/login/widgets/loginviewbody.dart';
import 'package:flutter/material.dart';

class loginView extends StatelessWidget {
  const loginView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: loginviewbody(),
    );
  }
}
